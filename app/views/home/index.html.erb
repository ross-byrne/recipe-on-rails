<% data = {
    root_url: request.base_url,
    starting_recipes: @all_recipes }
%>
<%= javascript_tag do %>
    // Save data in js for react to pick up
    window.data = <%= data.to_json.html_safe %>
<% end %>

<div id="app" class="h-full"><div>
